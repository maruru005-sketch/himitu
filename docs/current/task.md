# タスク一覧および進捗状況

- `[ ]` 未完了
- `[/]` 進行中
- `[x]` 完了

## 【完了】Phase 7.2（その他の追加機能）およびUI改善
### バックエンド実装 (`server.py`)
- `[x]` VWAP の計算ロジック追加（当日の累積）
- `[x]` ATR (True Range) の計算ロジック追加
- `[x]` `/api/chart/{code}` に `interval` パラメータを追加（1m, 5m, 15m, 30m, 60m, 1d, 1wk対応）
- `[x]` バックテスト機能 `GET /api/backtest/{code}` の作成
- `[x]` 銘柄リスト (`config.json`) の読み書き機能追加
- `[x]` LINE通知 API `POST /api/notify` の作成

### フロントエンド実装 (`index.html`)
- `[x]` 複数時間足の切り替えドロップダウンの追加
- `[x]` チャートへのVWAPライン（オレンジ色）の描画
- `[x]` SL設定での「固定%」と「ATRベース」の切り替えUI追加
- `[x]` バックテスト結果を表示するモーダルUIの作成
- `[x]` 「設定」ボタンと銘柄・LINE連携設定用モーダルUIの作成
- `[x]` サーバー連携のアラート通知実装（デスクトップ＆LINE連携）

### UI/UXの改善・その他（本日の追加対応）
- `[x]` 起動用バッチファイル (`start.bat`) の作成
- `[x]` 全体的なフォントサイズの拡大（12px -> 13px）による視認性向上
- `[x]` 右パネル（POSITION等）の横幅をドラッグでリサイズできる機能の追加
- `[x]` 出来高グラフの高さ・色の調整による視認性向上
- `[x]` POSITIONパネルの余白拡大と、それに伴う SIGNAL LOG の配置最適化
- `[x]` **監視銘柄設定の簡略化** (New!):
  - JSON形式を廃止し、企業番号（4桁）を入力するだけで名前とティッカーを自動取得する機能を実装。

---

## 【完了】Phase 8: 実運用に向けた調整・検証
- `[x]` **平日の相場稼働時間における動作検証**:
  - 米国株（NVDA）を利用し、リアルタイム（30秒間隔）のデータ更新と指標計算が正常であることを確認。
- `[x]` **通知機能の実地テスト**:
  - 銘柄・種別ごとに通知間隔を制御するロジック（スロットリング）を導入し、過剰な通知を抑制。
- `[x]` **UI/UXの最終微調整**:
  - クラス名の割当バグ修正や、アラートしきい値の調整（0.1%）を実施。

---

## 【完了】Phase 9: 長期運用・利便性向上のための拡張
実運用を続ける中で、さらに利便性を高めるための拡張機能です。

- `[x]` **データの永続化 (Persistence)**:
  - SIGNAL LOG や現在のポジション情報を、バックエンド（`server.py`）で保存・復元できるようにする。
- `[x]` **パフォーマンス最適化**:
  - 監視銘柄スキャンのバックエンドでのデータ取得を並列化（Thread）して応答速度を維持。
- `[x]` **インジケーターの拡充**:
  - 現在計算のみ行っているMACDをチャート上に描画する機能の追加。
- `[x]` **トレード成績の統計表示**:
  - 日ごとの損益合計を算出し、簡単なレポートとして表示する機能。

---

## 【完了】開発環境の強化（2026-04-30）
AIとの共同開発をより安全・高速に進めるための土台作りが完了しました。

- `[x]` **Gitによるバージョン管理の導入**:
  - プロジェクト全体の履歴管理を開始。不測の事態でも過去の状態に復元可能に。
- `[x]` **GitHub (himitu リポジトリ) との連携**:
  - クラウド上へのバックアップ体制を構築。
- `[x]` **開発効率化ツールの検討**:
  - Linter/Formatter (Ruff, Prettier) や、将来的なブラウザ拡張機能のアイデア出し。

---

## 【完了】Phase 10: 短期取引向けのおすすめ銘柄（スクリーニング）機能
ユーザーが手動で設定した監視銘柄とは別に、短期トレードに向いている銘柄を自動で見つけて表示する機能を追加します。

- `[x]` **おすすめ銘柄の抽出ロジック (バックエンド)**:
  - 出来高急増や値上がり率ランキング上位、あるいは特定のテクニカル指標（MACDゴールデンクロスなど）を満たす銘柄を自動でスキャンしてピックアップする API エンドポイントを作成する。
- `[x]` **おすすめ銘柄表示UI (フロントエンド)**:
  - 左側の監視銘柄リストとは別に、「おすすめ銘柄（急騰銘柄）」を一覧表示できるエリア（またはタブ切り替え）を作成する。

---

## 【完了】コード品質改善（2026-05-01）
`server.py` の Pylance/Pyright 警告をすべて解消しました。

### PEP8 / コードスタイル修正
- `[x]` **E741**: 曖昧な変数名 `l` → `l_val` / `low` に改名（calc_bollinger, calc_atr）
- `[x]` **E402**: 関数内の `import re` をファイル先頭へ移動
- `[x]` **E302**: トップレベル関数間の空行不足（11箇所）を修正
- `[x]` **E701**: 単一行の `if`/`continue` ステートメントを複数行に分割（4箇所）

### 未使用コードの除去
- `[x]` 未使用の `timedelta` インポートを削除
- `[x]` 未使用の `pd` エイリアス → `import pandas` に変更（型解決のため維持）
- `[x]` 未使用変数 `df` → `_df` に改名（backtest_api）

### Pyright 型チェック対応
- `[x]` `list[None]` → `list[float | None]` 型アノテーション追加（calc_sma, calc_rsi, ema, calc_atr）
- `[x]` `from __future__ import annotations` 追加
- `[x]` None 安全チェック追加（`assert`, `or 0` ガード）
- `[x]` pandas 型スタブの制限 → `# type: ignore` で抑制（tz_convert, strftime, row アクセス）
- `[x]` `pyrightconfig.json` 追加で `.venv` パッケージ解決を有効化

### 検証結果
- `[x]` `pyright`: **0 errors, 0 warnings, 0 informations** ✅
- `[x]` `py_compile`: **Syntax OK** ✅

---

## 【次回】残課題・今後の作業

### 優先度：高
- `[x]` **index.html の JavaScript 警告対応**:
  - VS Code の Problems パネルに残っている可能性のある JS 警告の確認と修正。
  - `== null` → `=== null` の厳密等価への変換検討（意図的な null/undefined 両方チェックかどうか確認）。
- `[x]` **動作確認テスト**:
  - server.py の型修正後、実際にサーバーを起動して全機能（チャート表示・おすすめ・バックテスト・ポジション管理）が正常動作するか検証。
- `[x]` **Git コミット**:
  - 今回のコード品質改善をコミット＆プッシュ。

### 優先度：中（Phase 11 進行中）
- `[/]` **AIを活用した「ニュース・決算要約」パネル**:
  - `[x]` Google News RSS 等から銘柄ニュースを取得するバックエンドロジックの実装
  - `[x]` Gemini API 連携ユーティリティの実装（要約・感情分析）
  - `[x]` フロントエンドでの要約カード UI 実装と文字サイズの大幅拡大
  - `[x] APIクォータ制限対策：クライアント・サーバー両面での永続キャッシュ実装
- `[/]` **トレードノート（日誌）の自動生成機能**:
  - `[x]` 画像保存用ディレクトリ (`files/logs/`) の作成
  - `[x]` フロントエンドでのチャートキャプチャ (Canvas to DataURL) 実装
  - `[x]` エントリー・決済時に画像をサーバーへ送信・保存する API の実装
  - `[ ]` 過去のトレード画像を表示するダッシュボード UI の実装
- `[ ]` **ブラウザ拡張機能（Chrome Extension）連携**:
  - `[ ]` 外部サイトからの銘柄追加用ブックマークレットの作成
  - `[ ]` 簡易的な Chrome 拡張機能のプロトタイプ作成

### 優先度：低（継続改善）
- `[ ]` **Ruff / Prettier の導入**:
  - 自動フォーマッタを導入してコードスタイルを統一し、今後の警告発生を予防。
- `[ ]` **テストコードの追加**:
  - テクニカル指標計算（calc_sma, calc_rsi, calc_macd 等）の単体テストを追加し、ロジックの正確性を担保。
